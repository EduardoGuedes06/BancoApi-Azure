@model IEnumerable<Banco.MVC.ViewModel.ContaJuridicaViewModel>

@{
    ViewData["Title"] = "Lista de Contas Juridicas";
}

<h1>@ViewData["Title"]</h1>

<p supress-by-claim-name="ContaJuridica" supress-by-claim-value="Adicionar">
    <a class="btn btn-info" asp-action="Create">Nova Conta</a>
</p>

<table class="table">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CNPJ)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataCriacao)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.CNPJ)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.DataCriacao)
            </td>
            
            <td class="text-right">
                <a class="btn btn-info" asp-controller="ContaJuridica" asp-action="Details" asp-route-id="@item.Id"><spam class="fa fa-search"></spam>Detalhes</a>              
                <a class="btn btn-danger" supress-by-claim-name="ContaJuridica" supress-by-claim-value="Excluir"  asp-action="Delete" asp-route-id="@item.Id"><spam class="fa fa-trash"></spam>Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
